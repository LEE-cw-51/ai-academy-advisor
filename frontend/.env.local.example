# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Naver Maps JavaScript API client ID (optional — map shows placeholder if missing)
NEXT_PUBLIC_NAVER_MAP_CLIENT_ID=
