{
  "name": "AI Academy Advisor (학원콕)",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && PATH=\"$HOME/.local/bin:$PATH\" uv run uvicorn app.main:app --host 0.0.0.0 --port 8000",
      "description": "FastAPI backend API on http://localhost:8000 (docs at /docs)"
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev",
      "description": "Next.js frontend on http://localhost:3000"
    }
  ],
  "ports": [
    { "name": "frontend", "port": 3000 },
    { "name": "backend", "port": 8000 }
  ]
}
